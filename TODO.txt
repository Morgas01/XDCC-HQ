overall
-	close dialogs via "esc" key
-	plugins
-	dashboard?

download
-	fix: abort download and directly start next one from the same bot (xdcc cancel time lag) ?
-	clean name: don't replace period between numbers

irc  
-	irc user list?
-	accept xdcc downloads

config
-	history max count
-	accept only requests from localhost
